packages:
  - landing
  - server

allowBuilds:
  better-sqlite3: true
  esbuild: true
  # Runs from the prebuilt @img/sharp-* binary. The install script only compiles from source,
  # which sharp attempts whenever a system libvips exists, and fails without node-addon-api.
  sharp: false
  workerd: true
