# GitHub Sponsors is unavailable here (Georgia is outside Stripe's supported
# countries), but `custom` needs no eligibility — it just links out. That is what
# puts the Sponsor button on the repo.
#
# NOWPayments, crypto-only: no merchant KYC on the payout side. The donor-facing
# card path is an on-ramp with its own minimums, so this is a sponsor rail rather
# than a tip rail — see ROADMAP.md Phase 4.
custom: ["https://nowpayments.io/donation/asmyshlyaev177"]
